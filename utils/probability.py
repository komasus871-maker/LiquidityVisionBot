class Probability:

    @staticmethod
    def calculate(score):

        if score >= 95:

            return 98

        if score >= 90:

            return 95

        if score >= 80:

            return 90

        if score >= 70:

            return 82

        if score >= 60:

            return 74

        if score >= 50:

            return 65

        return 45